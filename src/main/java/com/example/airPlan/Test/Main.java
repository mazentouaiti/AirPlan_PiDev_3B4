package com.example.airPlan.Test;
/*import model.event;
import service.eventService;
import model.ticket;
import service.ticketService;

import java.sql.Date;
import java.util.List;

*/
public class Main {

    public static void main(String[] args) {
  /*
   ticketService st = new ticketService();

        eventService se = new eventService();

        //ticket
            //add
         //   ticket t =new ticket(223, "DDDD", 44, "FZ",22);
            st.add(t);
            System.out.println(" Ticket ajouté avec succès !");

        //delete
            st.delete(12);

            System.out.println(st.display());

            //affichage

        List<ticket> tickets = st.display();/*
        System.out.println("\nListe des ticket :");
        for (ticket i : tickets) {
            System.out.println(i);
        }
            //update
        if (!tickets.isEmpty()) {
            ticket ti = tickets.get(0);
            ti.setId_ticket(19);
            ti.setStock_ticket(999);
            st.update(ti);
            System.out.println("Ticket mis à jour avec succès !");
        } else {
            System.out.println("Aucun ticket à mettre à jour.");
        }

        //event

            //add
            event e = new event( "zzzzzzzzzzzz", "SOIREE", Date.valueOf("1000-10-10"), "OOO", "MARRSA", "TTTT");
            se.add(e);
        System.out.println(" Event ajouté avec succès !");
            //delete
        se.delete(25);
            System.out.println(se.display());


   //affichage
        List<event> events = se.display();


        System.out.println("\nListe des events :");
        for (event i : events) {
            System.out.println(i);
        }
        //update
            if (!events.isEmpty()) {
                event eUpdate = events.get(0);
                eUpdate.setNom_event("tapi");
                eUpdate.setId_event(35);
                se.update(eUpdate);
                System.out.println("\nevent mis à jour avec succès !");
            } else {
             System.out.println("Aucun événement à mettre à jour.");
         }
*/
    }
}


